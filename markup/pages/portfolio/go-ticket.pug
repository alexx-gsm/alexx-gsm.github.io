doctype html
html(class="no-js", lang="ru")

    include ../../components/head/head.pug
    include ../../components/logo/logo.pug
    include ../../components/menu/menu.pug
    include ../../components/project/project.pug
    include ../../components/menu-page/menu-page.pug

    head
      +head(head.defaults)
    body#page.page(class="page--" + head.defaults.value)
      |%=symbols=%

      // START page wrapper
      div.page__wrapper
        // START page header
        header.page__header
          +logo()
          +menu(topMenu, 'Портфолио')
        // END page header

        // START page content
        div.page__content(id="content")
          .page__img.blur
            .page__img-wrap
              != pugHelpers.Icon.call(locals, {iconName: 'ivolga-bg', className: 'icon icon--ivolga'})
          //- START PROJECT
          +project(portfolio.goticket)         
          //- END PROJECT
          .section.section--project.mt-100
            .section__title
              h2 GoTicket
            .section__subtitle 
              | Приложение для бронирования мест в ресторанах и покупки подарочных сертификатов
        // END page content

        // START MENU PAGE
        +menu-page(topMenu, 'Портфолио')
        // END MENU PAGE
      
      // END page wrapper
          

      script(src='%=static=%js/separate-js/svg4everybody.min.js')
      script svg4everybody();

      //- Main scripts. You can replace it, but I recommend you to leave it here
      script(src='%=static=%js/main%=hash=%%=min=%.js')